{
  "name": "openapigeneratortest",
  "version": "1.0.0",
  "scripts": {
    "clear": "rm -rf dist",
    "generate": "openapi-generator-cli generate -c './config/config.yaml'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.6.0"
  }
}
