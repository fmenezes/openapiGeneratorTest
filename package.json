{
  "name": "openapigeneratortest",
  "version": "1.0.0",
  "scripts": {
    "clear": "rm -rf dist",
    "init": "mkdir -p ./dist && cp ./config/.openapi-generator-ignore dist/.openapi-generator-ignore",
    "generate": "npm run init && openapi-generator-cli generate -c './config/config.yaml'"
  },
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.6.0"
  }
}
